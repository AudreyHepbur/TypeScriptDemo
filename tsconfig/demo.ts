
/**
 * 
 * removeComments
 * 编译时是否显示注释（注解），值设置为true也就是编译出来的js文件不显示注释内容
 * 
 */
// I love jspang
var _parent: string  = 'jspnag'
console.log(_parent);


/**
 * 
 * noImplicitAny
 * 允许你的注解类型 any 不用特意表明
 * 
 */
function Person(name) {
  return name
}

/**
 * 
 * strict
 * true,就代表我们的编译和书写规范，要按照TypeScript最严格的规范来写
 * 
 */

 
 /**
  * 
  * strictNullChecks
  * 设置为false 不强制检查 NULL 类型
  * 
  */
const Jspang:string  = null


/**
 * 
 * {
    "outDir": "./build" ,
    "rootDir": "./src" ,
  }
 * 
 * outDir 配置编译生成的JS文件存放目录
 * rootDir 指定被编译的ts文件存放目录
 * tsc命令编译
 * 
 */


 /**
  * 
  * noUnusedLocals：true  无法编译未使用的变量
  * noUnusedParameters：true  无法编译未使用的参数
  */
const jspang: string = null
export const name = 'jspang'